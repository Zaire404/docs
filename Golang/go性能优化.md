## 重用内存
a := b[:0]
```go
// 示例1：使用 a := b[:0]
b := []int{1, 2, 3}
a := b[:0]    // len=0, cap=3
a = append(a, 4) // a=[4], b=[4,2,3]

// 示例2：使用 a := b
b := []int{1, 2, 3}
a := b       // len=3, cap=3
a = append(a, 4) // a=[1,2,3,4], b=[1,2,3]
```
